# jekyll-plugin-example

```
docker-compose up -d
docker ps
docker exec -it <CONTAINER ID> bash
```

```
bundle exec jekyll serve --watch --force_polling --verbose --incremental --host=0.0.0.0 --trace
```

```
JEKYLL_ENV=production bundle exec jekyll build
```
